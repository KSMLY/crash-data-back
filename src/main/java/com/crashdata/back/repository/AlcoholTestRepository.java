package com.crashdata.back.repository;

import com.crashdata.back.entity.AlcoholTest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AlcoholTestRepository extends JpaRepository<AlcoholTest, Long> {
}
